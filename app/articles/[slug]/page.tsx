import { notFound } from "next/navigation";
import { CalendarBlank, Clock } from "@phosphor-icons/react/dist/ssr";
import { PageHeader } from "@/components/PostCard";
import { articles, currentUser } from "@/lib/data";
export function generateStaticParams(){return articles.map(({slug})=>({slug}))}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const article=articles.find(a=>a.slug===slug);if(!article)notFound();return <><PageHeader title="Article" back/><article className="article-detail"><img className="article-cover" src={article.thumbnail} alt=""/><h1>{article.title}</h1><div className="article-by"><img src={currentUser.avatar} alt=""/><span><b>Ira Desai</b><small><CalendarBlank/>{article.date}<i>·</i><Clock/>{article.readTime}</small></span></div><p>{article.caption}</p><h2>It Started as a Simple Idea</h2><p>I didn’t begin with a big launch plan. I wanted to make something genuinely useful, then kept refining the details until the experience felt clear and effortless.</p><p>The work grew through small, deliberate decisions: listening closely, removing friction, and focusing on the parts that matter to people using the product every day.</p><h2>Build, learn, repeat</h2><p>Good work is rarely one perfect decision. It is a series of thoughtful iterations, honest feedback, and the willingness to keep improving the core idea.</p></article></>}
