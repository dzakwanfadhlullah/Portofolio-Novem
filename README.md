# SocialFolio

A responsive Next.js and React implementation of the SocialFolio Framer project, including the feed, profiles, stories, articles, notifications, and store.

## Development

```bash
npm install
npm run dev
```

Verify the production build with `npm run lint` and `npm run build`.
